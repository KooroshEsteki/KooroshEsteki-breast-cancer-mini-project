# Breast Cancer Campaign Mini Project

This mini project uses a pre-processed breast cancer dataset saved as `data_refined.csv`.

The goal is to classify breast tumors as malignant or benign using machine learning classifiers.

## Dataset

The dataset file used in this project is:

```text
data_refined.csv
